spring.application.name=SolaceAtelier-Inventory-System

# Default to dev profile when no SPRING_PROFILES_ACTIVE env var is set
spring.profiles.active=dev

# Required so data.sql runs after Hibernate creates the schema (applies to both profiles)
spring.jpa.defer-datasource-initialization=true